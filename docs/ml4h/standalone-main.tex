% LAST RESORT ONLY. Use the official ML4H 2026 template if it compiles at all.
%
% The body below is already pasted in. This is a self-contained document that produces a correctly formatted two-page
% spec sheet using nothing but packages every TeX installation already has. It
% exists so that a broken template project cannot cost the submission: paste the
% body of spec-sheet.tex where marked, compile, submit.
%
% What it does NOT carry is the ML4H class's own styling and the \mlhtrack{demo}
% marker. Reviewers read the content; a submission that compiles beats a
% submission that does not.

\documentclass[10pt,letterpaper]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{times}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{microtype}

\setlength{\parindent}{1.5em}
\setlength{\parskip}{0pt}

% ML4H Demo Track, single blind: the submission is NOT anonymised.
\title{\vspace{-2em}AfyaCore: On-Device Speech-to-Structured Clinical Records
for Health Facilities Without Reliable Connectivity}
\author{Mohammed Sameer Syed\\ \texttt{sdmohammedsameer@gmail.com}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
AfyaCore is an offline-first web application that turns a spoken consultation
into a structured, encrypted clinical record on the clinician's own phone, for
primary-care facilities in sub-Saharan Africa. Speech recognition (Whisper
base) and de-identification (OpenMed PII) run on the device; nothing leaves it
unless the facility configures synchronisation. A deterministic extractor
turns the transcript into typed fields, and nothing machine-derived enters the
record until a clinician confirms it beside its source phrase. The system is
functional end to end and technically validated, with field accuracy measured
from speech through the exact model files the phone runs. It has not yet been
evaluated with clinicians.
\end{abstract}

\section{Introduction}

Primary-care facilities across sub-Saharan Africa record consultations on
paper: written once, hard to find at the next visit, and reaching national
reporting as a hand-tallied monthly aggregate. Digitisation fails on
constraints of the setting: intermittent connectivity, low-end Android
hardware, and staff who cannot spend a consultation typing. AfyaCore is a data-capture tool for nurses, midwives and clinical officers at
health posts and health centres. The clinician dictates in French or English; on-device speech
recognition produces a transcript, a deterministic extractor parses vital
signs, diagnosis and prescriptions into typed fields, and each field is shown
with its source phrase and confidence for the clinician to confirm. It does not diagnose, triage or recommend treatment.

\section{Method}

\textbf{Architecture.} A single-page progressive web application with an
optional self-hosted synchronisation server. The initial blocking load is
154\,kB gzipped, a service worker precaches the shell, and the application is
fully functional with the network disabled.

\textbf{Speech recognition.} OpenAI Whisper \texttt{base} (72.6M parameters)
\citep{radford2022whisper}, exported to ONNX, quantised to int8 (81\,MB) and
run in a Web Worker through \texttt{transformers.js} on WebAssembly. Without it, dictation is disabled rather than sent to a cloud API.

\textbf{Extraction and review.} The transcript is parsed by a deterministic,
locale-aware rule system rather than a language model, whose failure mode is a plausible wrong number
with nothing downstream to catch it. Each value carries a plausibility range,
is dropped if outside it, and is confirmed individually beside the phrase it
came from; a later change makes it pending again, and an unconfirmed value is
withheld from the export and the monthly aggregate.

\textbf{De-identification and encryption.} Exports pass through a
deterministic stage that removes every identifier the device holds for the
patient, then an optional on-device neural stage (OpenMed PII French Clinical,
33M parameters, 70\,MB) \citep{openmed2025} for identifiers the roster has
never seen. At rest, clinical stores are AES-GCM ciphertext under a key wrapped by each
clinician's PIN (PBKDF2, 600{,}000 iterations) and held in memory only for
the session.

\textbf{Data.} A synthetic corpus of 22 cases, scored as text and again as
text-to-speech audio through the same model files, extractor and scorer the
phone runs; clinical-content retention is scored against gold
\texttt{CLINENTITY} annotations in the E3C corpus \citep{magnini2021e3c}.

\section{Results}



\begin{table}[h]
\centering\small
\begin{tabular}{lrr}
\toprule
Speech to fields (22 synthetic cases) & French & English \\
\midrule
Extraction F1, written transcript & 100\% & 100\% \\
Extraction F1, \emph{from synthetic speech} & 67\% & 91\% \\
\quad extractor as first shipped & 36\% & 60\% \\
Raw word error rate & 60\% & 48\% \\
Wrong values a flag-first reviewer reaches & 91\% & 100\% \\
\quad at the previous threshold & 18\% & 20\% \\
\midrule
De-identification & determ. & +\,neural \\
\midrule
Identifiers \emph{on} roster removed & 100\% & 100\% \\
Identifiers \emph{off} roster removed & 0\% & 40\% \\
Clinical retained, real French (n=1258) & 100\% & 98.3\% \\
Clinical retained, real English (n=1014) & 100\% & 99.3\% \\
\bottomrule
\end{tabular}
\caption{Synthetic speech is a controlled condition, not a bound. Off-roster
recall is 0\% by construction for exact matching.}
\end{table}

The system is at version 0.0.2, deployed publicly under MIT, and has \emph{not}
been piloted in a facility; the evidence is technical validation. It carries
632 automated tests and a fourteen-step offline walk that drives the production
build through a real browser, transcribing both languages before and after a
disconnected reload and asserting identical transcripts. Nine country profiles and patient instruction sheets in ten
languages, including Kiswahili, Wolof, Hausa and Malagasy, are shipped.

\section{Discussion}

\textbf{Measuring the pipeline, not its parts, found the defects.} An
extractor at 100\% and a recogniser with a published word error rate said
nothing about fields from speech until the two were scored together. That
cost 33 to 40 points of F1 and showed where: the recogniser writes a comma
where a full stop was spoken, so a diagnosis swallows the prescription after
it, and it spells drugs as it hears them (\emph{par assez tamol} for
\emph{parac\'etamol}). Each failure is now handled and pinned by a test.

\textbf{A threshold nobody chose is not a control.} The flag for uncertain
values was \texttt{confidence} $<$ \texttt{0.8}, and the extractor gives every diagnosis
and chief complaint exactly 0.8, so the two fields a recogniser mangles most
could never be flagged; a reviewer reading flagged rows first reached one
error in five. Likewise, a correction lands in a record that is already final,
so an unconfirmed diagnosis reached a research export and a district malaria
count. Both were found by measuring a control rather than reading it.

\textbf{The neural scrub is weakest where the tool is aimed, and it eats
diagnoses.} All three off-roster identifiers it missed were Malagasy: a French
model with an English WordPiece vocabulary fragments Malagasy proper nouns. On
real French text it cost 2.4\% of gold clinical entities before a guard was
added, including \emph{lymphome malin non hodgkinien}, because Hodgkin is a
surname. Neither finding was reachable on a corpus we wrote ourselves.

\textbf{Hindsight and next.} An external review found five release blockers,
each a place where documentation flattered behaviour; the worst was dictation
streaming audio to the browser vendor under a claim of no third-party call.
The system was built to a technical standard before it was put in front of a
clinician, which is the wrong order; next is a formative study at one facility
and consented role-play recordings from intended users. Translation review, FHIR validation and server-side encryption remain open.

\textbf{In-person demonstration.} The application installs from a URL on any
modern phone and runs entirely offline with a synthetic workspace, dictation
included, so it runs on attendees' own devices with no network and no real
patient data. Its evaluation also ships as a screen, where an attendee runs the model on a
case and sees each field's match, flag, and how a correction changes the score.

% References do not count against the 2-page limit.
%
% Written out inline rather than as \bibliography{refs}, so this document opens
% no external file and needs no BibTeX pass. A missing refs.bib is a TeX file
% error, which aborts the run with "Emergency stop" and produces no PDF at all.
\begin{thebibliography}{3}

\bibitem[Radford et~al.(2022)]{radford2022whisper}
A.~Radford, J.~W. Kim, T.~Xu, G.~Brockman, C.~McLeavey, and I.~Sutskever.
\newblock Robust speech recognition via large-scale weak supervision.
\newblock \emph{arXiv:2212.04356}, 2022.

\bibitem[OpenMed(2025)]{openmed2025}
OpenMed.
\newblock {OpenMed} {NER}: Open-source clinical named entity recognition.
\newblock \emph{arXiv:2508.01630}, 2025.

\bibitem[Magnini et~al.(2021)]{magnini2021e3c}
B.~Magnini, B.~Altuna, A.~Lavelli, M.~Speranza, and R.~Zanoli.
\newblock The {E3C} project: European clinical case corpus, 2021.
\newblock doi:10.57771/dey2-g751.

\end{thebibliography}

\end{document}
